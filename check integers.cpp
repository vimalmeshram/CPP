#include<iostream>
using namespace std;
int main(){
	int a, b;
	cout<<"Enter two integers:";
	cin>>a>>b;
	if(a==b){
		cout<<"Both integers are equal";
	}else{
		cout<<"Both integers are different";
	}
}
